var athletes = require("./athletes.json").athletes.filter(function(d){
  return d.gender === "M" && d.medals.length && d.medals.some(function(d){
    return d.fillColor === "#fea90d";
  });
});

console.log(athletes.length);
